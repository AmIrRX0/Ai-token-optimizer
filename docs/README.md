# Screenshots & demo assets

Drop your marketing/demo media here and they'll render in the README:

- `hero.png` — main screenshot of the live counter + compress button on a chat page
- `demo.gif` — short GIF of compressing a prompt and watching the token count drop
- `popup.png` — the toolbar popup
- `options.png` — the settings page

Suggested width: 1280px for screenshots, keep GIFs under ~5 MB.

> These are referenced by `../README.md`. Until you add them, the README shows
> placeholder links — replace them with real captures before publishing.
